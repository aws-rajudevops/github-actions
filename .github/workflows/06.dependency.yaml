name: Dependency
# When to trigger this pipeline. We can trigger on push or manual or someother events like creating PR, updating PR, creating branches, creating issues
on: [push] # triggers
jobs: # Pipeline. One workflow can have multiple pipelines
  job-1:
    runs-on: ubuntu-latest # Linux OS. We can have windows or MAC
    steps: # stages
      - name: step1
        run: echo "hi, iam from job-1"
  job-2:
    needs: job-1
    runs-on: ubuntu-latest # Linux OS. We can have windows or MAC
    steps: # stages
      - name: step1
        run: echo "hi, iam from job-2"
  job-3:
    needs: job-2
    runs-on: ubuntu-latest # Linux OS. We can have windows or MAC
    steps: # stages
      - name: step1
        run: echo "hi, iam from job-3"
  job-4:
    needs: job-3
    runs-on: ubuntu-latest # Linux OS. We can have windows or MAC
    steps: # stages
      - name: step1
        run: echo "hi, iam from job-4"
